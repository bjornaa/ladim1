examples/data

Some of the examples uses a forcing file, ocean_avg_0014.nc, from a ROMS simulation.

This dataset is available with anonymous ftp from ftp.imr.no/bjorn/ladim-data
or simpler by running the attached download script
